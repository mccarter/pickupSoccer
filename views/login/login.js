angular.module('pickupSoccer.login', [])

.controller('LoginController', function($scope){

		 // var ref = new Firebase("https://<your-firebase>.firebaseio.com/");
		  // download the data into a local object
		  //$scope.data = $firebaseObject(ref);

});
